/*
 TP2 - exercice 2
 
 Patrice Béchard p1088418 BECP30119404
 
 20 mars 2016
 */
//on vérifie si le header n'est pas déjà défini avant de le définir. 
#ifndef DEF_VOITURE_H
#define DEF_VOITURE_H
#include <string>//pour utiliser le type string
//on définit notre classe voiture
class Voiture{
	private:
		int _numeroSerie;
		int _sorte;
		int _annee;
		float _prix;
	public:
		Voiture();
		Voiture(int numeroSerie, int sorte, int annee, float prix);
		Voiture(Voiture const &laVoiture);
		~Voiture();
		int getNumeroSerie();
		int getSorte();
		int getAnnee();
		float getPrix();
		void setNumeroSerie(int numeroSerie);
		void setSorte(int sorte);
		void setAnnee(int annee);
		void setPrix(float prix);
		std::string afficher();
};

#endif
